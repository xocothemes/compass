---
interface Props {
  previews: Record<string, string>;
}

const { previews } = Astro.props;
const previewJson = JSON.stringify(previews).replace(/</g, '\\u003c');
---

<script is:inline type="application/json" id="docs-search-previews" data-docs-search-previews set:html={previewJson} />
