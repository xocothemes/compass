---
title: 'Steps'
description: 'Use numbered steps when readers need to follow a process in a strict sequence.'
category: 'components'
order: 7
updatedAt: 2026-06-03
---

import { Image } from 'astro:assets';
import placeholderImage from '../../compass-docs/adding-images/docs-image-placeholder.png';

Steps work best when order matters and each item includes more than a one-line instruction.

## Use in MDX

Use `Steps` as the wrapper and `Step` for each item:

```mdx
<Steps>
  <Step number={1} title="Install dependencies">
    Run `npm install`.
  </Step>
  <Step number={2} title="Start the dev server">
    Run `npm run dev`.
  </Step>
</Steps>
```

## Implementation notes

The wrapper and item components live in `src/components/docs/Steps.astro` and `src/components/docs/Step.astro`. The visual styling is controlled by the `.docs-step*` rules in `src/index.css`.

## Preview

<Steps>
  <Step number={1} title="First Step">
    These are instructions or content that only pertain to the first step.

    <Image
      src={placeholderImage}
      alt="Placeholder illustration displayed beneath the first step content"
      width={1200}
      layout="constrained"
    />

  </Step>
  <Step number={2} title="Second Step">
    These are instructions or content that only pertain to the second step.
  </Step>
  <Step number={3} title="Third Step">
    These are instructions or content that only pertain to the third step.
  </Step>
</Steps>
