---
title: "Callout Boxes"
description: "Use compact callout boxes to highlight information, warnings, confirmations, and breaking changes."
category: "components"
order: 1
updatedAt: 2026-06-03
---

Callouts are useful when writers need to interrupt the reading flow on purpose and make a note impossible to miss.

## Use in MDX

Add a `Callout` block directly inside any `.mdx` article:

```mdx
<Callout tone="info">
  This adds a note in the content.
</Callout>
```

You can also provide an optional title:

```mdx
<Callout tone="warning" title="Before you rename routes">
  Add redirects for any URLs that already exist in production.
</Callout>
```

The `title` prop is optional. Use it when the callout needs a stronger label, and leave it out when a short note is enough on its own.

## Implementation notes

The component is registered in `src/components/docs/mdx-components.ts`, so it is already available inside docs articles. If you want to change the styling or tones, edit `src/components/docs/Callout.astro` and the matching callout styles in `src/index.css`.

## Tones

- `info` for general guidance
- `success` for confirmation states
- `warning` for risky steps
- `danger` for breaking changes

## Preview

<Callout tone="info">
  This adds a note in the content.
</Callout>

<Callout tone="success" title="You are ready to publish">
  Use this for publishing confirmations, approved changes, or moments where the reader should feel confident moving on.
</Callout>

<Callout tone="warning">
  Reserve this for migrations, redirects, deletions, or any step that can cause confusion if skipped.
</Callout>

<Callout tone="danger">
  Save the danger tone for irreversible edits, removed features, or updates that can take part of the docs offline.
</Callout>
