---
title: 'Publish Compass'
description: 'Prepare Compass for production, verify the build output, and publish with confidence.'
category: 'start-here'
order: 5
updatedAt: 2026-06-05
---

Publishing Compass is mostly about replacing the sample project details in the repository you created from the template, verifying the production build, and checking that the generated site behaves the way you expect.

## Update the public-facing settings

Before publishing, review `site.config.mjs` and make sure these values are yours:

- `siteUrl`
- `name`
- `title`
- `description`
- `githubUrl`
- `navCtaLabel`
- `navCtaHref`
- `footerText`

These settings affect metadata, navigation labels, links, canonical URLs, and the site URL used by the generated RSS feed and sitemap.

Compass is intended to ship as your own docs repository. The root `package.json` is marked private so the project is not accidentally published as an npm package.

## Test the production build locally

Search is generated during the build step, so it is worth testing the production output instead of relying only on the dev server.

Run:

```bash
npm run build
npm run preview
```

Use that preview to confirm:

- the homepage sections look correct
- category and article routes load
- search returns results
- `/rss.xml` includes the updates you expect to expose
- metadata-driven content such as titles and descriptions appears correctly

## Check your content structure

Before going live, make sure:

- sample articles you do not want are removed or rewritten
- category names in `src/data/docs.ts` match your real information architecture
- article ordering feels intentional
- links between articles still make sense after any renames

## Review assets and branding

Compass ships with default icons and social assets. Replace the ones you want to own before publishing:

- `public/favicon-light.svg`
- `public/favicon-dark.svg`
- `public/og-image.png`

## Final pre-publish checklist

<Checklist>
  <ChecklistItem>Build succeeds with `npm run build`</ChecklistItem>
  <ChecklistItem>Search works in `npm run preview`</ChecklistItem>
  <ChecklistItem>`site.config.mjs` uses real URLs</ChecklistItem>
  <ChecklistItem>Sample docs have been replaced</ChecklistItem>
  <ChecklistItem>Branding assets are updated</ChecklistItem>
  <ChecklistItem>Your repository was created from the template and renamed for your docs site</ChecklistItem>
</Checklist>

After that, Compass is ready to deploy as a static site on the hosting platform you prefer.
